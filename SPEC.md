# 报文字节格式（前后端唯一规矩）

Go（`frame` 包）和页面（`web/codec.js`）都按本文件实现，两边对同一份字节
必须吐出结构相同、顺序相同的结果。任一边不一致即为缺陷。

## 1. 字节序与整数

- 所有多字节整数一律**小端**（least significant byte first）。
- 变长整数（varint）一律为 **LEB128**：
  - 每字节低 7 位为有效载荷，最高位（bit 7）为续位；
  - 续位为 1 表示后面还有字节，为 0 时本整数结束；
  - 先出现的字节是低位（`value += (byte & 0x7f) << (7 * i)`）；
  - 单字节且值 < 128 时最高位天然为 0；
  - 没有“首字节写长度”这一说，长度只看续位。

## 2. 帧布局

```
帧       := u16le(version)  field*
field    := varint(id)  u8(kind)  payload
kind = 1 标量：payload = u32le(value)
kind = 3 成组：payload = varint(byte_len)  field*（恰好 byte_len 个字节）
```

- 字段一个接一个排列到帧末，没有外层长度、没有结束标记。
- 成组的 `byte_len` 只限定其内层字段占用的字节范围，不参与字段取值。
- `id` 取自下方固定字典；字典里没有的名字一律为“未知”，但 `id` 原样保留。

| id  | 名字 |
|-----|------|
| 1   | 电压 |
| 2   | 成组 |
| 3   | 湿度 |
| 4   | 气压 |
| 5   | 风速 |
| 6   | 云量 |
| 8   | 计数 |
| 150 | 温度 |

## 3. 输出结构与顺序

- 解析结果为 `{ version, fields }`。
- `fields` 是**数组**，严格按字节出现顺序排列；同 `id` 重复出现就重复保留，
  不做去重、不进 map、不依赖任何哈希或对象键顺序。
- 每个字段为 `{ id, name, value }`：
  - 标量字段 `value` 为数字（u32，范围 0 ~ 4294967295）；
  - 成组字段 `value` 为内层字段数组，规则同上。
- 编解码对称：`encode(decode(b))` 对所有合法报文得到与 `b` 完全相同的字节；
  `decode(encode(f))` 得到与 `f` 等价、顺序一致的结果。

## 4. 截断与非法输入

- 不足 2 字节：返回 `version = 0`、`fields = []`。
- 解析途中任一字段越界（varint 跑到末尾、标量不足 4 字节、成组长度越界、
  kind 无法识别）：立即停止，**丢弃当前这个不完整字段**，保留此前已完整解析
  的字段，绝不抛错、绝不 panic。

## 5. 对照基线

`web/sample.bin`（48 字节）按本规矩解析应得：

```
version = 0x0001
fields:
  id=2 成组 [
    id=3 湿度 5
    id=4 气压 6
    id=5 风速 7
    id=6 云量 8
  ]
  id=1   电压 1000
  id=8   计数 3
  id=150 温度 7
```

这条期望同时以硬编码字节写在 Go 测试（`frame/`）和 JS 测试（`web/codec.test.js`）
里；Go 测试还会直接运行 JS 测试产物做对拍，两边对不上即失败。
