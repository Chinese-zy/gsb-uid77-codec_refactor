# 报文编解码规矩

页面（web/codec.js）和接口（frame/decode.go）都按这一份来，唯一标准是
`testdata/golden.json` 里的字节对照表，两边测试都跑它，对不上就不许交。

## 帧结构

```
+------------+--------------------------+
| version:2B | 字段序列（直到帧尾）     |
+------------+--------------------------+
```

- version：无符号 16 位整数，**小端**。
- 字段序列：一个挨一个的字段，没有字段计数，读到帧尾为止。

## 字段

```
+-----------+---------+--------+
| key:varint| kind:1B | value  |
+-----------+---------+--------+
```

- key：字段编号，varint（见下）。名称由编号查表得到，查不到一律叫 `未知`。
- kind：1 字节，决定 value 怎么读。
  - `1`：无符号 32 位整数，**小端**，4 字节。
  - `3`：嵌套字段序列。先读一个 varint 长度 n，再读 n 字节，
    这 n 字节按同一份规矩递归解成字段序列。
  - 其他：帧已损坏，停止解析，保留已解出的字段。

## varint

LEB128：每字节低 7 位是有效数据，最高位为 1 表示后面还有字节，
先到的字节是低位。例：`96 01` = 0x16 + 0x01*128 = 150。

## 顺序

- 字段按字节流里的先后顺序输出，重复 key 全部保留，不去重、不排序、不按 key 重排。
- 嵌套字段同理。

## 边界与截断

- 嵌套字段的所有读取不得越过它声明的 n 字节边界，递归时以该边界为帧尾。
- 字段读到一半不够字节（key、kind、u32、嵌套长度越界）：停止解析，
  保留已解出的字段，不报错、不补零。

## 输出

JSON 形状固定：

```json
{"version": 1, "fields": [{"name": "电压", "value": 1000}]}
```

- 嵌套字段的 `value` 是同形状的字段数组。
- 字段列表为空时输出 `[]`，不许输出 `null`。
